# Shire UI env (Vite). Usually nothing is needed:
# - dev: the Vite proxy forwards /api → localhost:8000 (same-origin, base URL stays empty)
# - Docker: nginx proxies /api → backend (same-origin, base URL stays empty)
# Set only when a deployed build must call an API on a different origin.
#VITE_API_BASE_URL=
